[project]
name = "agentic-ai-mcp-platform"
version = "0.1.0"
description = "Agentic AI + MCP Platform with a Software Engineering focus"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
]


[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

"pydantic-settings>=2.2",

